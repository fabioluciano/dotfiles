[user]
name = "{{ .name }}"
email = "{{ .email }}"

# Override email for work repositories (Bitwarden so e consultado em maquina de trabalho)
{{- if eq (includeTemplate "is-work-machine.tmpl" .) "true" }}
[[--scope]]
--when.repositories = ["/mnt/srv/work"{{ if eq .chezmoi.os "darwin" }}, "~/Projects/work"{{ end }}]
{{- if env "WORK_MAIL" }}
[--scope.user]
email = "{{ env "WORK_MAIL" }}"
{{- else }}
{{- $fields := bitwardenFields "item" "dotfiles-tokens" }}
[--scope.user]
email = "{{ $fields.work_mail.value }}"
{{- end }}
{{- end }}

[ui]
editor = "nvim"
pager = "delta"
default-command = "log"

[git]
write-change-id-header = true

[remotes.origin]
auto-track-bookmarks = "*"

[aliases]
# View
l = ["log"]
ll = ["log", "-r", "all()"]

# Status
s = ["status"]
st = ["status"]

# Other
b = ["bookmark", "list"]
